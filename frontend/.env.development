# Development environment configuration
# API URL - use relative path to leverage Vite's proxy
VITE_API_URL=/api

# Note: Backend URL is now auto-detected based on environment
# - Development: Uses Vite proxy (relative paths to /api and /uploads)
# - Production: Uses window.location.hostname with port 5011
