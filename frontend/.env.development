# Development environment configuration
# API URL - use relative path to leverage Vite's proxy
VITE_API_URL=/api

# Backend URL for direct file access (audio, images, etc.)
# This is used for media files that aren't proxied through /api
VITE_BACKEND_URL=http://15.207.18.106:5000
