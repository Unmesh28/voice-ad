import { Box, Typography } from '@mui/material';

const Production = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Audio Production
      </Typography>
      <Typography variant="body1" color="text.secondary">
        Audio production page - Coming in Phase 5
      </Typography>
    </Box>
  );
};

export default Production;
